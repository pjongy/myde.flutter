flutter
  $ flutter

  Check flutter development preparation
    $ flutter doctor -v

  Run debugger
    $ flutter run

adb
  $ adb

  Turn on debugging server on device(while usb debugging)
    $ adb tcpip 5555

  Connect via tcp (5555 port)
    $ adb connect {IP}
